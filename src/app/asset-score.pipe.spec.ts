import { AssetScorePipe } from './asset-ratio-calculator/asset-score.pipe';

describe('AssetScorePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetScorePipe();
    expect(pipe).toBeTruthy();
  });
});
